# CLAUDE.md

Antes de cualquier acción, leer obligatoriamente:

1. `specs/SPEC-0000-normas-del-proyecto.md` — normas del proyecto: stack, convenciones de código, flujo Git, reglas de testing y formato de SPECs.

## Preferencia de versión de API de HubSpot

Al elegir endpoints de HubSpot, usar siempre la versión más alta disponible para el recurso, con este orden de prioridad:

`2026-03` (API por fecha) > `v4` > `v3` > `v2` > `v1`

Nota: la discrepancia de `phone_number` (admitido como `type` en la referencia `2026-03` de propiedades, pero descrito como `string` + `fieldType: phonenumber` en la guía legacy) se debe al cambio de versión de API; se resuelve adoptando la versión más alta.

## SPECs del proyecto

Todos los SPECs están en `specs/`. Estado actual:

| SPEC | Título | Estado |
|------|--------|--------|
| [SPEC-0000](specs/SPEC-0000-normas-del-proyecto.md) | Normas del Proyecto | VALIDADO |
| [SPEC-0001](specs/SPEC-0001-fundacion-del-proyecto.md) | Fundación del Proyecto | IMPLEMENTADO |
| [SPEC-0002](specs/SPEC-0002-app-shell.md) | App Shell | IMPLEMENTADO (patrón transversal de estados de carga y respuesta inmediata/A11y §17 —Skeleton/CircularProgress + `useAsyncResource` + reset al abrir; norma en SPEC-0000 §3; adopción registrada en SPEC-0003 §14, 0004 §17, 0005 §13, 0006 §27, 0007 §18, 0008 §27, 0010 §12, 0011 §12—, IMPLEMENTADO (conciliado 2026-06-24; `useAsyncResource` + `LoadingState` y adopciones presentes en código); tooltip i18n de campos rellenables §18 —componente compartido `FieldTooltip` + hook `useFieldHelp`; norma en SPEC-0000 §3; claves `<superficie>.fieldHelp.<campo>` en es/ca/eu/en; adopción en SPEC-0003 §15, 0004 §20, 0005 §14, 0006 §31, 0007 §19, 0008 §28—, IMPLEMENTADO 2026-06-23, JSON locales validado, typecheck/test en máquina; botones de acción con icono §19 —norma transversal: todo `Button` con texto lleva `startIcon` (los `IconButton` solo-icono intactos); mapeo verbo→icono y detalle botón a botón en §19.3; 76 botones en 26 ficheros de SPEC-0002/0003/0004/0005/0006/0007/0008/0010/0011—, IMPLEMENTADO 2026-06-23, typecheck/test en máquina —el espejo del sandbox corrompe la codificación de los ficheros editados, originales sanos—; ErrorBoundary de ruta §20 —`RouteErrorBoundary` compartido como `errorElement` en las rutas de proyecto (Dashboard/CRM/Propiedades/Objetos/Formularios); contiene fallos de render sin tumbar la app; i18n `errorBoundary.*` en los 7 locales; IMPLEMENTADO 2026-06-25, typecheck/test en máquina—; endurecimiento de navegación/enlaces externos §22 —solo `http:`/`https:` en `openExternal` + guard `will-navigate`; IMPLEMENTADO 2026-07-02, typecheck/test en máquina—; modularización del wiring del main §23 —handlers IPC por feature en `src/main/ipc/*`, `drive-docs.ts` (write*Sheets + refresh) y helper `createProjectRecord<T>` en `shared/project-record.ts` adoptado por los 5 stores por proyecto; refactor sin cambio de comportamiento, canales IPC intactos; IMPLEMENTADO 2026-07-02, requiere rebuild de la app, typecheck/suite en máquina—) |
| [SPEC-0003](specs/SPEC-0003-conector-hubspot.md) | Conector HubSpot | IMPLEMENTADO (refresco automático al cambiar de entorno §16 —hook compartido `useHubspotEnvironmentChange` + adopción en Propiedades/Objetos/Formularios/Dashboard/Vista CRM; IMPLEMENTADO 2026-06-25, suite/typecheck en máquina por truncación del espejo—; endurecimiento del proxy IPC y paths §17 —allowlist de prefijos en `hubspot:request` (403 fuera de `/crm/`, `/marketing/v3/forms`, `/account-info/`) + `encodeURIComponent` en los segmentos dinámicos de path (properties/schemas/forms); IMPLEMENTADO 2026-07-02, typecheck/test en máquina—; reintentos coherentes con el rate limiting §18 —`retryDelayMs` (`Retry-After` > 10 s en 429 > backoff 5xx) + hook `onRetry` que descuenta reservoir del limiter (sin reencolar: riesgo de deadlock con `maxConcurrent`); IMPLEMENTADO 2026-07-02, requiere rebuild de la app/MCP—; `hubspotErrorMessage` compartida §19 —`connectors/hubspot/errors.ts` con sujeto del 409 parametrizado; los 3 servicios delegan y formularios/objetos custom ganan el mapeo 401/403/429/409/400; IMPLEMENTADO 2026-07-02—; catch en `useHubSpotConnector` §20 —`.catch`+`.finally` en la carga de estado, sin spinner infinito ante fallo IPC; IMPLEMENTADO 2026-07-02—) |
| [SPEC-0004](specs/SPEC-0004-conector-google-drive.md) | Conector Google Drive | IMPLEMENTADO (credenciales por UI §13 + selector propio sin Picker §14; patrón común de documentos Drive §15; hipervínculo «Abrir en Drive» §18 —solo el Sheets legible, en DriveDocActions y lista del conector, 2026-06-23; 111/111 + tsc en verde en sandbox—; revisión y actualización automática de los archivos de Drive al abrir el proyecto §19 —enmienda §15.2 solo en local→Drive; `gdrive:refresh-project` + orquestador `drive-refresh.ts` + aviso por Snackbar; 72/72 + tsc en verde en sandbox, 2026-06-23—; §17 estados de carga pendiente; corrección documentos de estado vacíos y duplicados §21 —`createManagedDocument` resiliente + borra huérfanos, reutiliza Doc existente y deduplica, `write*Sheets` comprueba el resultado de `writeFile`; IMPLEMENTADO 2026-06-24, `client.spec.ts` 15/15 + tsc en verde en sandbox, diagnóstico del fallo de `docsBatchUpdate` (runtime) y suite/e2e en máquina pendientes—; soporte de unidades/carpetas compartidas §22 —`supportsAllDrives`/`includeItemsFromAllDrives` en todas las llamadas Drive `files.*` de `client.ts` y `sheets-client.ts`; resuelve `File not found` al guardar en Shared Drives; IMPLEMENTADO 2026-06-25, requiere rebuild de la app, test:unit en máquina por truncación del espejo—; endurecimiento de logs y queries §24 —`error.message` en vez del GaxiosError completo (fuga de access token en logs) + `quote()` en todas las queries `files.list` de `client.ts`/`sheets-client.ts`; IMPLEMENTADO 2026-07-02, requiere rebuild de la app—; correctitud del conector §25 —paginación `nextPageToken` en todos los listados (`listAllFiles`), timeout de 5 min en el loopback OAuth, refresco de token deduplicado por proyecto + mensaje claro en `invalid_grant`, y retry con backoff 429/5xx (`retry.ts`, `retried()` sobre los façades Drive/Docs/Sheets); IMPLEMENTADO 2026-07-02, requiere rebuild de la app—; escritura de Sheets en batch §26 —`valuesBatchClear`/`valuesBatchUpdate` sustituyen el clear+update por hoja (N×2 → 2 llamadas por libro); IMPLEMENTADO 2026-07-02—; feedback de error en `DriveDirtyGuard` §27 —fallo de `onUpdate` notificado por Snackbar, el diálogo permanece; IMPLEMENTADO 2026-07-02—; typecheck/test en máquina) |
| [SPEC-0005](specs/SPEC-0005-capa-mcp-api.md) | Capa MCP / API | IMPLEMENTADO (fix simetría CRUD MCP §11.1, 2026-06-18; borrado de grupos diferido; typecheck/test en máquina; gate de guía obligatoria §15 —tool `revops_guidance` + registro de secciones por feature + `requiresGuidance` con acuse por `sessionId`; bloquea tools de escritura/sync hasta leer la guía; `guidance.spec.ts` 4/4 en sandbox—, 2026-06-25, requiere rebuild MCP, suite/typecheck en máquina por truncación del espejo; transparencia del gate §15.11 —el `message` de `guidance-required` explicita la semántica real (acuse por sesión, sin umbral de escrituras, rearme al reconectar/reiniciar); del informe de remapeo de erratas punto 4; IMPLEMENTADO 2026-06-30, mcp 25/25 + tsc en verde en sandbox, requiere rebuild MCP—; endurecimiento de seguridad §16 —try/catch en los handlers HTTP/SSE (un rechazo ya no tumba el proceso main) + token MCP cifrado con `safeStorage` (`tokenEncrypted`, migración transparente desde claro, fallback si el SO no cifra); IMPLEMENTADO 2026-07-02, requiere rebuild MCP, typecheck/test en máquina—; cierre limpio al salir §17 —`before-quit` espera `mcpService.stop()` con tope de 3 s antes de `app.quit()`; IMPLEMENTADO 2026-07-02—; validación runtime del inputSchema + criterio del gate + `requiredScopes` informativo §18 —`validate-input.ts` (`required`/`type`/`enum` de primer nivel) en `callTool`, devuelve `{error:{code:'invalid-input',issues}}` sin ejecutar el handler; criterio homogéneo: toda tool que muta estado (incl. descartes) o sincroniza lleva `requiresGuidance` (adopción en SPEC-0006 §48, 0007 §20, 0008 §29); `requiredScopes` documentado como INFORMATIVO (H6 sigue diferido); IMPLEMENTADO 2026-07-02, requiere rebuild MCP, typecheck/test en máquina—) |
| [SPEC-0006](specs/SPEC-0006-gestion-de-propiedades.md) | Gestión de Propiedades | IMPLEMENTADO (rediseño §16; patrón común Drive §21; fix defectos MCP §22.4 —validación originId + origins_delete—, 2026-06-18; ampliación de tipologías §25 —display hints de número (moneda/porcentaje/duración), formato de texto, calculadas, valor único, sensibilidad, calculation_equation; constantes compartidas con SPEC-0007; requiere rebuild MCP—, 2026-06-22; hallazgos batería MCP §26 RESUELTOS H1–H5 —sync resiliente, grupo en entorno destino al aplicar, discard valida id, opciones bool, calc fuera del diff— H6 diferido (scope token), requiere rebuild MCP, 2026-06-22; reordenación de la UI §25.7 —sección colapsable «Opciones avanzadas» en EntryWizard—, 2026-06-22; opciones de enumeración en diálogo aparte §25.8 —OptionsDialog con scroll propio, búsqueda y pegado masivo—, 2026-06-22; migración a CRM Properties API 2026-03 §28 —paths `/crm/properties/2026-03/...`, resuelve `phone_number`; requiere rebuild MCP—, 2026-06-22; borrado (archivado) de propiedades como cambio pendiente §29 —tool `properties_request_delete` + operación `delete`; requiere rebuild MCP—, 2026-06-22; typecheck/test en máquina; hoja de definición completa por objeto §32 —hojas `Definicion` (20 columnas mapeadas a `HubSpotPropertyDef`) y `DefOpciones`; ver SPEC-0012 §12; IMPLEMENTADO 2026-06-24, `sheets-model.spec.ts` 11/11 en sandbox, suite/typecheck en máquina—; borrado de grupos de propiedades §33 —retira el diferido §22.3; destructivo; IMPLEMENTADO 2026-06-24 (conector `deleteGroup`, `GroupDeleteChange` + store local, servicio request/apply —precondición grupo vacío—/discard, 4 tools MCP + tests; IPC/preload `groups:*`; UI «Grupos» en `PropertyManagementScreen` vía `GroupsModal` con doble confirmación y panel de pendientes; i18n es/ca/eu/en; requiere rebuild MCP; verificación en máquina por truncación del espejo); groupChanges no portados al companion de Drive—; descripción de propiedad en el UI §34 —campo movido al núcleo del `EntryWizard` (fuera de «Opciones avanzadas») y bloque de solo lectura en `EntryPanel`; `EntryPanel.spec.tsx`; i18n sin cambios (clave `properties.panel.description` ya existía en los 7 locales); IMPLEMENTADO 2026-06-24, typecheck/test en máquina por truncación del espejo—; diagnóstico de «falta» y «Convertir a Nueva» §35 —`reconcile` separa el bloqueo (modo `existing` apuntando a propiedad inexistente) del `missing` con remedio; `summary.blocked` + `blockers[]` en `properties_sync` y `properties_pending_changes`; acción «Convertir a Nueva» individual/masiva (`convertEntryToNew`/`convertMissingToNew`) + 2 tools MCP gated; banner UI + tooltip; i18n en los 7 locales; adopta el gate SPEC-0005 §15; IMPLEMENTADO 2026-06-25, requiere rebuild MCP, suite/typecheck/e2e en máquina por truncación del espejo—; fix divergencia perpetua de enumeraciones §36 —`optionsEqual` y `cleanOptions` normalizan `hidden ?? false`; resuelve el `update_options` infinito en catálogos grandes (País de Nacimiento, Compañía) con opciones importadas sin `hidden`; IMPLEMENTADO 2026-06-25, requiere rebuild de la app/MCP, test:unit en máquina por truncación del espejo—; estado siempre contra Producción §37 —`syncHubspot` reconcilia con `listProperties(objectType, 'production')`; el entorno activo solo gobierna la aplicación de cambios; IMPLEMENTADO 2026-06-25, requiere rebuild de la app/MCP, test:unit en máquina—; create ya existente en el entorno → reconcilia y aplica update §38 —`applyChange` absorbe «already exists» (409/OBJECT_ALREADY_EXISTS) y reconcilia contra la propiedad del entorno destino aplicando el `update` equivalente (`diffDefinition`+`patchProperty`), no-op si coincide; resuelve el fallo al aplicar en sandbox una propiedad que existe en sandbox pero falta en producción; IMPLEMENTADO 2026-06-25, requiere rebuild de la app/MCP, test:unit en máquina—; endurecimiento de `entries_upsert` y definición real vía MCP §39 —validación del `entry` (rechaza `hubspotProperty` string y `sources` strings/originIds), `inputSchema` enriquecido, `destName` defensivo (reconcile/UI), nueva tool de solo lectura `hubspot_properties_list` (devuelve `fieldType`/`groupName`/`options` reales, evita inferir), guía MCP ampliada; del informe 2026-06-25; IMPLEMENTADO 2026-06-25, requiere rebuild de la app/MCP, test:unit/e2e en máquina—; pulido de mensajes de error §39.9 —errores estructurados+acumulativos (`entry-validation.ts`: `validateEntryInput`/`EntryValidationError` con code/field/example), `entries_upsert` devuelve `{error:{issues}}`, `hubspotErrorMessage` mapea 401/403/429/409/400, i18n `errors.entryValidation` en 7 locales; `entry-validation.spec.ts` 4/4 en sandbox; IMPLEMENTADO 2026-06-25, requiere rebuild de la app/MCP—; remapeo de erratas §40–§45 —§40 doc de `valueMap`/mapeo de opciones (`options.sourceValue→hubspotValue`, `boolean`) en `PROPERTY_GUIDANCE`; §41 idempotencia de `upsertEntry` (resetea cambios huérfanos al cambiar destino/definición); §42 tools batch `entries_upsert_batch`/`entries_delete_batch`/`properties_discard_changes_batch`; §43 propiedades de sistema no-creables (`system-properties.ts`, blocker `system-property`/`relink`); §44 validación longitud(100)/patrón + colisión de creaciones de `hubspotName`; §45 filtro por nombre en el listado (`filters.search` en 7 locales); del informe de remapeo de erratas; IMPLEMENTADO 2026-06-30, property-management 49/49 + mcp 25/25 + tsc en verde en sandbox, requiere rebuild de la app/MCP—; exposición formal de `formField` §46 —disponibilidad en formularios y chatbots por MCP (schema `entries_upsert` new/existing + guía + parseo remoto en `RemoteProperty`/`toDef` para evitar divergencia perpetua) y UI (Switch en «Opciones avanzadas» del `EntryWizard` + bloque de solo lectura en `EntryPanel`); 6 claves i18n × 7 locales; IMPLEMENTADO 2026-07-02, pending-changes 16/16 + EntryPanel 5/5 + property-management/mcp 109/109 + typecheck node/web en verde en sandbox, requiere rebuild de la app/MCP—; correctitud del servicio §47 —relectura del store tras los await de red en `applyChange`/`syncHubspot` (evita last-write-wins con ediciones concurrentes UI+MCP, patrón de forms) + `updateOrigin` valida el id y devuelve el origen fusionado real; IMPLEMENTADO 2026-07-02, requiere rebuild de la app/MCP, typecheck/test en máquina—; gate de guía en descartes §48 —`requiresGuidance` en `properties_discard_change`/`_batch`/`properties_groups_discard_change` (criterio SPEC-0005 §18.2); IMPLEMENTADO 2026-07-02, requiere rebuild MCP—; listados del sync en paralelo §49 —`Promise.allSettled` sobre `listProperties` por objeto, semántica H1 intacta; IMPLEMENTADO 2026-07-02—; manejo de errores en la UI §50 —`handleApply` comprueba el retorno (sin toast verde en fallo), catch+`error` en entries/origins/objects stores, `EntryWizard` con `saving`+BusyButton y Snackbar en submit/createGroup, try/catch en convertAll/export; IMPLEMENTADO 2026-07-02, typecheck/test en máquina—) |
| [SPEC-0007](specs/SPEC-0007-objetos-custom-hubspot.md) | Objetos Custom de HubSpot | IMPLEMENTADO (documento Drive §15; fix creación end-to-end vía MCP §16.4 —custom_objects_sync + delete_draft—, 2026-06-18; adopción del gate de guía §20 —`requiresGuidance` en upsert_draft/apply_change/discard_change/sync/delete_draft; IMPLEMENTADO 2026-07-02, requiere rebuild MCP—; typecheck/test en máquina) |
| [SPEC-0008](specs/SPEC-0008-gestion-de-formularios.md) | Gestión de Formularios | IMPLEMENTADO (patrón común Drive §15; fix defectos MCP §16.4 —normalize create_definition + forms_discard_change—, 2026-06-18; fix mapeo tipos de campo a Forms API v3 §19 —checkbox/booleancheckbox/date—, 2026-06-19; fix validation requerido en campos email §20 —requiere rebuild MCP—, 2026-06-19; edición de formularios update_form §21 + origen al crear explícito/validado §22 —requiere rebuild MCP—, 2026-06-19; edición de cambios pendientes §23 —forms_edit_pending_change, requiere rebuild MCP—, 2026-06-19; consentimiento legal con Subscriptions API v4 §24 —privacyText/checkboxes, forms_subscription_types, requiere rebuild MCP—, 2026-06-19; fix campos requeridos archived/createdAt/updatedAt en el cuerpo §25, 2026-06-19; fix límite de 3 campos por grupo §26, 2026-06-19; gate de guía + validación de orígenes en upsertLink §29 —`requiresGuidance` en las 7 tools de escritura/sync y `assertOriginsExist` en `upsertLink`/`forms_link_origin`; IMPLEMENTADO 2026-07-02, requiere rebuild MCP—; cache de plantillas de consentimiento §30 —TTL 5 min por proyecto/entorno/tipo en `getConsentTemplate` (antes descargaba todo el portal por applyChange); IMPLEMENTADO 2026-07-02—; typecheck/e2e + PR en máquina) |
| [SPEC-0009](specs/SPEC-0009-tutoriales-multidioma.md) | Tutoriales Multidioma | VALIDADO — implementado (typecheck/test/e2e + PR pendientes en máquina) |
| [SPEC-0010](specs/SPEC-0010-dashboard-de-estado.md) | Dashboard de Estado | IMPLEMENTADO (mejoras UX alto impacto, 2026-06-19; typecheck en verde, suite unitaria completa pendiente en máquina) |
| [SPEC-0011](specs/SPEC-0011-vista-general-crm.md) | Vista General de CRM | IMPLEMENTADO (2026-06-19; typecheck/lint en verde, suite unitaria completa pendiente en máquina) |
| [SPEC-0012](specs/SPEC-0012-identidad-visual-documentos-drive.md) | Identidad Visual de los Documentos de Drive | IMPLEMENTADO (2026-06-23; módulo de marca compartido `brand.ts`; Sheets de propiedades con bloque por objeto + hoja `01_Indice` y `SHEETS_SCHEMA_VERSION` 2→3 §2.3; estilo de marca en cabeceras/portada/validación/formato condicional `sheets-style.ts` §3.1; portada de Docs estilada `cover-template.ts` §3.2 sin alterar el round-trip SPEC-0004 §15.5; adopción en SPEC-0006 §30 / SPEC-0007 §18 / SPEC-0008 §27; 172/172 tests + `tsc --noEmit` en verde en sandbox, suite completa + e2e + PR en máquina; enmienda §12 —hoja `Definicion` completa por objeto + `DefOpciones`, `SHEETS_SCHEMA_VERSION` 3→4; IMPLEMENTADO 2026-06-24, sandbox en verde, suite/e2e en máquina—; numberFormat por columna §13 —dirigido por datos en `buildStyleRequests`; IMPLEMENTADO 2026-06-24, test/typecheck en máquina—) |
| [SPEC-0013](specs/SPEC-0013-guardado-y-portabilidad-de-proyectos.md) | Guardado y Portabilidad de Proyectos | IMPLEMENTADO (2026-06-23; archivo `.rvproj` portable —contenedor ZIP método STORE propio `project-file/archive.ts` sin nueva dependencia §11—; manifest.json + `sections/<featureKey>.json`; registry de contribuyentes extensible §4.2; saneado de secretos §2.6; preservación de secciones desconocidas/nuevas vía `Project.portableSections` §2.4; integridad sha256/checksum; IPC `projects:export/import-*` + diálogos nativos; UI exportar en `ProjectCard` + importar en bienvenida (`ImportProjectDialog`, «Crear copia» por defecto) §3; i18n `projectFile.*` en es/ca/eu/en; tutoriales `doc/tutoriales/project-file/`; 14/14 tests del núcleo + `tsc` aislado del módulo en verde en sandbox; typecheck completo/suite/e2e/JSON-load + PR en máquina —el espejo del sandbox corrompe los ficheros editados, originales sanos—; validación del archivo en import §12 —`readProjectFile`: extensión `.rvproj` + archivo regular + máx 50 MB antes de leer/parsear; IMPLEMENTADO 2026-07-02, typecheck/test en máquina—) |
| [SPEC-0014](specs/SPEC-0014-idiomas-gallego-portugues-frances.md) | Idiomas Gallego, Portugués y Francés | IMPLEMENTADO (2026-06-24; `SUPPORTED_LANGUAGES`/`LANGUAGE_AUTONYMS` ampliados a 7 idiomas con `gl`/`pt`/`fr`; `locales/{gl,pt,fr}/common.json` con paridad de claves total contra `es` —759 claves, 0 faltantes/extra— y registro en `i18n/index.ts`; tutoriales a paridad 29×7=203 —cierra el gap previo de `crm`/`dashboard` en `ca`/`eu`/`en` y añade el tutorial `hubspot/scopes-hubspot.md` (creación de PAT con enlaces + scopes por característica + agrupado)—; `check-tutorial-parity.mjs` a 7 idiomas y `config.spec.ts` actualizado; paridad y JSON validados en sandbox; typecheck/test:unit/e2e + PR en máquina. Nota: el espejo del sandbox truncó la lectura de `hubspot/es/crear-private-app.md` —original sano, 37 líneas—, por lo que `pt`/`fr` se recompletaron; paridad de `common.json` §12 —75 claves añadidas a ca/eu/en (bloques properties.wizard/newProp/kinds/fieldTypes/entry/panel/originsModal + common.loading/retry/loadError), script `check-locale-parity.mjs` + scripts npm `check:locales`/`check:tutoriales`, `defaultValue` 'Cargando…' eliminado (EntryWizard/LoadingState) y fecha de gdrive.lastSync con `Intl.DateTimeFormat(i18n.language)`; IMPLEMENTADO 2026-07-02, 6/6 locales a paridad en sandbox, typecheck/test en máquina—) |
| [SPEC-0015](specs/SPEC-0015-manual-de-usuario-pdf.md) | Manual de Usuario (PDF) | IMPLEMENTADO (2026-06-26; `doc/manual/Manual-Usuario-RevOps-CloudDistrict.pdf` —39 págs A4, fuentes embebidas— consolidando los 28 tutoriales canónicos `es`; identidad CD (paleta, Poppins + serif italic, ritmo dark/light, sin bullets, lima solo badge/numeración); 13 mockups SVG de marca que recrean las pantallas; generado con Python en `venv` + WeasyPrint 69; serif Liberation Serif Italic por ausencia de Libre Baskerville y de red a Google Fonts; contraste AA verificado; scripts de build en el espacio temporal, entregable el PDF. Nota: el espejo del sandbox truncó `content.py`/`build_manual.py` al escribir no-ASCII —reparado en sandbox, PDF correcto—) |

Tooltip i18n en campos rellenables (norma transversal) IMPLEMENTADO (2026-06-23): regla en SPEC-0000 §3 (i18n/a11y), componente compartido `FieldTooltip` + hook `useFieldHelp` y test en `renderer/shared/components/feedback/` (SPEC-0002 §18), claves `<superficie>.fieldHelp.<campo>` en es/ca/eu/en, adopción en SPEC-0003 §15, SPEC-0004 §20, SPEC-0005 §14, SPEC-0006 §31, SPEC-0007 §19, SPEC-0008 §28. JSON de los cuatro locales validado; typecheck/test/e2e + PR en máquina.

Botones de acción con icono (norma transversal) IMPLEMENTADO (2026-06-23): definida en SPEC-0002 §19. Todo `Button` con texto lleva `startIcon` de `@mui/icons-material`; los `IconButton` solo-icono se mantienen. Mapeo verbo→icono (§19.2) y detalle botón a botón con icono asignado (§19.3): 76 botones en 26 ficheros (App/shared, conectores HubSpot/Drive/MCP, Propiedades, Objetos, Formularios, Dashboard, CRM). `BusyButton` ya soportaba `startIcon`. Sustitución 1:1 sin cambio de lógica ni claves i18n; única desviación del borrador: `EntryPanel` :137 usa `CheckCircle` (no `Delete`). Verificación typecheck/test:unit + PR en máquina —el espejo del sandbox corrompe la codificación de los ficheros editados (no-ASCII como `→`; también afecta a ficheros no tocados), originales verificados sanos vía lectura directa—.

Mejoras de UX de alto impacto (`INFORME-ux-2026-06-19.md`) IMPLEMENTADAS (2026-06-19): SPEC-0002 §10 (Snackbar global) y §11 (ConfirmDialog compartido) en `renderer/shared/components/feedback/`; adopción en SPEC-0003 §13, SPEC-0004 §16, SPEC-0005 §12, SPEC-0006 §23, SPEC-0007 §17, SPEC-0008 §17; y SPEC-0010 (Dashboard de estado). `npm run typecheck` en verde; suite unitaria completa pendiente de ejecutar en máquina.

Mejoras de UX de impacto medio IMPLEMENTADAS (2026-06-19): SPEC-0002 §13 (retirada de Mapas/Reporting del menú hasta tener SPEC), SPEC-0002 §14 (consistencia: `StatusBadge`/`EmptyState`/`SIDE_PANEL_WIDTH` compartidos en `renderer/shared/components/` + adopción) y SPEC-0011 (Vista General de CRM). Además se eliminaron cuatro e2e `test.fixme` (`export-json`, `forms-flow`, `link-origin`, `new-form`; ver SPEC-0006 §24 y SPEC-0008 §18). typecheck/lint en verde; suite unitaria completa pendiente en máquina.

Mejoras de UX de bajo impacto / higiene IMPLEMENTADAS (2026-06-19): SPEC-0002 §16 — indentación de las áreas de CRM en el sidebar (`NavItem.child`), `aria-hidden`/`aria-label` puntuales (selector de carpeta Drive y asistente de formularios) y borrado del fichero muerto `PropertiesTable.tsx`. typecheck/lint en verde.

Leer el SPEC correspondiente antes de implementar o modificar cualquier característica.

Cada nuevo SPEC aprobado debe añadirse a esta tabla antes de continuar con el siguiente.

## Alcance de cada SPEC

Límites estrictos para evitar solapamientos entre SPECs:

| SPEC | Alcance — qué hace | Fuera de alcance — qué NO toca |
|------|--------------------|-------------------------------|
| SPEC-0000 | Convenciones, stack, flujo Git, reglas de testing, formato de SPECs | Ningún código de producción |
| SPEC-0001 | Scaffolding Electron + TS + React, tema MUI con tokens CD, auto-updater, estructura de carpetas, READMEs, init Git con remote | Ninguna pantalla ni lógica de negocio |
| SPEC-0002 | Pantalla de bienvenida, selector de proyectos, menú lateral, topbar, layout principal, router, store de shell, sección Ayuda (visor in-app de los tutoriales de `doc/tutoriales/`) | Ningún conector externo; ninguna característica de negocio; no escribe el contenido de los tutoriales (lo aporta cada SPEC) |
| SPEC-0003 | Cliente HTTP HubSpot, autenticación PAT, gestión de entornos production/sandbox, rate limiting, pantalla de configuración del conector | No implementa ningún endpoint de negocio; no toca Google Drive ni MCP |
| SPEC-0004 | Cliente Google Drive, OAuth PKCE, selección de carpeta, sincronización, escritura/lectura de archivos con portada CD y versionado de esquema | No define la estructura de ningún archivo concreto (eso lo hace cada SPEC de característica); no toca HubSpot ni MCP |
| SPEC-0005 | Servidor MCP (stdio + HTTP/SSE), registry de tools, autenticación por token local, UI de configuración MCP | No implementa ninguna tool de negocio; las tools las registra cada SPEC de característica |
| SPEC-0006 | Mapa de propiedades HubSpot: orígenes de datos, entradas por objeto (nombre + propiedad HubSpot destino + orígenes con definición genérica y mapeo de opciones), selección de objetos (estándar + custom existentes), cambios pendientes en HubSpot, exportación JSON, Google Sheets, tools MCP de propiedades | No crea objetos custom (eso es SPEC-0007); no gestiona registros/instancias; no toca workflows ni sequences |
| SPEC-0007 | Creación y gestión de objetos custom de HubSpot (CRM Schemas API); catálogo de objetos para SPEC-0006 | No gestiona registros (instancias); no define las entradas de propiedades (eso es SPEC-0006) |
| SPEC-0008 | Formularios HubSpot (Marketing Forms API v3): importar (legacy + nueva), crear formularios solo-campos, **editar formularios existentes (update_form: campos, nombre, configuración, estilos, lógica, consentimiento; §21)**, asociar a orígenes de SPEC-0006 (también al crear vía MCP, §22), revisar cobertura, añadir campos en bloque, **editar cambios pendientes antes de aplicarlos (forms_edit_pending_change, §23)**, cambios pendientes/sincronización, volcado a Sheets, tools MCP de formularios | No borra formularios; no crea propiedades ni objetos (SPEC-0006/0007); no define orígenes; no gestiona submissions |
| SPEC-0009 | Internacionalización de los tutoriales: convención `doc/tutoriales/<feature>/<locale>/<slug>.md`, traducción de los tutoriales a `ca`/`eu`/`en`, visor de Ayuda consciente del idioma con fallback a castellano (`resolveContent`/`resolveTitle`, `help.fallbackNotice`), script de paridad | No crea tutoriales nuevos ni características; no cambia la mecánica de carga/render del visor (SPEC-0002); no toca i18n de la UI fuera de `help.fallbackNotice` |
| SPEC-0010 | Dashboard de estado del proyecto: pantalla índice del proyecto (solo lectura) que agrega estado de conectores (HubSpot/Drive/MCP) y contadores de cambios pendientes, con accesos directos y checklist de primeros pasos | No crea endpoints de escritura ni lógica de negocio; no duplica funcionalidad de las features (enlaza a ellas); el Snackbar/ConfirmDialog compartidos son de SPEC-0002 §10/§11 |
| SPEC-0011 | Vista general de CRM: pantalla índice de la ruta `crm` (solo lectura) con tarjetas de las tres áreas (Propiedades/Objetos/Formularios) — total + pendientes + acceso — y aviso si HubSpot no está conectado | No crea endpoints de escritura ni lógica de negocio; no duplica las features (enlaza a ellas); la retirada de Mapas/Reporting del menú es SPEC-0002 §13 |
| SPEC-0012 | Identidad visual de los documentos de Drive (transversal): módulo de marca compartido, estilo de los Sheets (banner, cabeceras, notas, validación/formato condicional, wrap, anchos), separación por objeto del mapa de propiedades + hoja índice, estilado de la portada del Doc de estado | No cambia el mecanismo de carga/escritura del conector (SPEC-0004) ni el round-trip §15.5; no añade pantallas ni controles en la app; no altera la lógica de negocio de las features |
| SPEC-0013 | Guardado y portabilidad de proyectos: archivo `.rvproj` (contenedor ZIP) versionado y extensible, registry de secciones por característica, export/import con resumen previo y estrategia de colisión, preservación de secciones desconocidas, integridad y saneado de secretos, IPC + UI de exportar/importar | No cambia dónde vive el estado operativo (electron-store SPEC-0002, documentos de Drive SPEC-0004 §15.5); no reimplementa los `serialize`/`parse` por característica (los reutiliza); no exporta credenciales; no añade lógica de negocio a las features |
| SPEC-0014 | Ampliación del conjunto de idiomas a 7 (`gl`/`pt`/`fr`): constantes `SUPPORTED_LANGUAGES`/`LANGUAGE_AUTONYMS`, `locales/{gl,pt,fr}/common.json`, registro en `i18n/index.ts`, traducción de tutoriales a paridad total, nuevo tutorial `hubspot/scopes-hubspot.md`, `check-tutorial-parity.mjs` y `config.spec.ts` | No cambia el mecanismo multidioma del visor (SPEC-0009) ni la mecánica de carga/render (SPEC-0002); no añade pantallas ni lógica de negocio; no toca HubSpot/Drive/MCP en runtime |
| SPEC-0015 | Manual de usuario en PDF (`doc/manual/`): consolida los tutoriales canónicos `es` en un documento de marca CD con mockups SVG, índice, glosario y orden de acciones; build con Python/venv + WeasyPrint | No reescribe ni sustituye los tutoriales de `doc/tutoriales/` ni el visor de Ayuda (SPEC-0002/0009); no añade pantallas ni lógica de negocio; no toca HubSpot/Drive/MCP en runtime |

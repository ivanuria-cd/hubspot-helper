# tests/unit

Tests unitarios transversales con **Vitest**. Los tests específicos de cada feature viven en `src/renderer/features/<feature>/tests/`.

Ejecutar: `pnpm test:unit`

Regla (SPEC-0000 §7): prohibido modificar tests aprobados sin un SPEC de modificación validado.

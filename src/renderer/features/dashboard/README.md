# features/dashboard (SPEC-0010)

Dashboard de estado del proyecto: índice de `/project/:id`. Solo lectura. Agrega estado de conectores (HubSpot/Drive/MCP) y contadores de cambios pendientes vía `useDashboardStatus`. Enlaza a las features; muestra checklist de primeros pasos si no hay conectores. Sin IPC nuevo.

/**
 * DEPRECADO (SPEC-0004 §14): el Google Picker se sustituyó por un selector de carpetas propio que
 * solo usa OAuth (sin API key). Este módulo se conserva vacío para no romper imports históricos.
 */
export {};
